---
name: fpf-local
description: Загрузка применимых принципов для задачи из иерархии Pack → SPF → FPF — локальная версия без сетевых MCP-вызовов (только Glob/Read/Grep). Используй когда нужно найти релевантные принципы, не отправляя текст запроса на внешний сервер.
argument-hint: "<запрос или тема>"
version: 1.2.0
status: active
triggers:
  slash: [/fpf-local]
  phrases: []
routing:
  executor: haiku
  deterministic: false
agents: single
interaction: multi-step
gates_required: []
gates_enforced: []
gates_rationale: "операционный скилл; WP Gate применим только при создании нового РП, не для операционных вызовов"
---

# Загрузка принципов (локально, без сети)

Запрос: $ARGUMENTS

## When to use

Загрузка применимых принципов для задачи из иерархии Pack → SPF → FPF. Используй когда нужно найти релевантные принципы перед принятием решения — и когда важно, чтобы текст запроса не уходил на удалённый сервер (личные/чувствительные формулировки).

## Fallback Chain

```
Pack (предметное) → SPF (корректность) → FPF (первые принципы)
```

## Режимы (определи по контексту запроса)

| Режим | Триггер | Что делать |
|-------|---------|-----------|
| **lookup** | Запрос содержит код паттерна (A.7, B.5, F.18) или конкретный термин | Grep по FPF → точная секция. Объясни в engineering-языке (см. `memory/fpf-reference.md` § Трансляция) |
| **review** | «Проверь», «сверь с FPF», артефакт для проверки | Загрузи артефакт + релевантные принципы → сверка пункт за пунктом → findings |
| **design** | «Спроектируй», «как сделать по FPF», задача проектирования | Подбери применимые паттерны (A.1 границы, A.6 состав, A.7 различения, B.5 ADI) → конкретные рекомендации |
| **characterize** | «Сравни», «оцени варианты», «как выбрать» | A.17-A.19 (Lawful Comparison): критерии → индикаторы → оценка вариантов по одинаковой шкале |
| **teach** | «объясни FPF», «что такое FPF», «с нуля», «введение» | Дай обзор FPF: что это, зачем, ключевые блоки (различения, паттерны, первые принципы) → объясни в engineering-языке без жаргона |
| **diagnose** | «какие принципы нарушаю», «что не так», «найди проблему», «проверь мою работу» | Получи описание работы/решения → найди расхождения с принципами FPF/Pack → назови конкретные нарушения с указанием принципа и где именно |
| **prioritize** | «наиболее важны», «для роли», «в роли», «для меня как» | Определи роль из аргумента → найди принципы FPF/Pack, релевантные этой роли → ранжируй по применимости → объясни почему каждый важен именно для этой роли |

**БЛОКИРУЮЩЕЕ — No-Jargon:** Каждый FPF-термин в output → engineering-эквивалент в скобках или вместо. Без исключений.
- Известные: `memory/fpf-reference.md` § Трансляция (Holon, F-G-R, Alpha, Mereology, Affordance, ADI...)
- Неизвестные (TransformerRole, senseFamily, Γ_*, ReferencePlane и т.п.): перевести по контексту, добавить `(FPF: <оригинал>)`. Не оставлять FPF-термин без перевода.
- **Тест перед выводом:** пробежать output — есть ли термин, непонятный инженеру без FPF? Если да — перевести.

## Algorithm

### 1. Определи режим

По таблице выше. Если неясно — режим lookup.

### 2. Классифицируй источник

- Предметное знание (архитектура, домен, сервис) → source_type=pack
- Форма, процесс, корректность → source=SPF
- Базовые различения, первые принципы → source=FPF

### 3. Ищи по файлам

- **FPF:** `FPF/FPF-Spec.md`. Целиком не читать — 12 МБ, спека это прямо запрещает. Вход: раздел `## Practical-Use Cards` (карточка распознаётся по ситуации, не по термину) → `# Table of Content` за идентификатором паттерна → тело паттерна (шаг 4).
- **SPF:** `SPF/docs/`, `SPF/spec/` через Glob + Read
- **Pack:** `PACK-*/pack/` через Glob + Read; репозиториев Pack нет — так и скажи, не подменяй предметное знание общими принципами

Порядок — по fallback chain (Pack → SPF → FPF): не нашёл на верхнем уровне — спускайся на следующий. Никаких сетевых вызовов (MCP) — только локальные файлы; так исключается путь, которым личный текст запроса мог бы уйти на удалённый сервер.

### 4. Открой паттерн — БЛОКИРУЮЩЕЕ

Спецификация предписывает дословно: «For any substantive answer, open the relevant pattern and apply its Problem frame, Solution, worked slices, and checklist to the project claim or object».

Идентификатор — адрес, а не ответ. Открой тело паттерна (`grep -n "^#\+ <ID>" FPF-Spec.md` → `sed -n` по найденному диапазону) и применяй к задаче его постановку проблемы, решение и чеклист. Тело паттерна само может указывать на другой, более точный паттерн (как A.19 указывает на `A.19.CPM`) — следуй за этим указанием, а не за первым совпавшим кодом.

**Запрет:** ссылаться на код паттерна, не открыв его в этой сессии. Опираешься на тёплую память (`memory/fpf-reference.md`, `hard-distinctions.md`) — пометь явно: «по памяти, не сверено». Тёплая память — указатель, а не источник: она дрейфует от спеки (найденные мёртвые ссылки: A.6.8, A.19).

### 5. Покажи результат

Релевантные принципы с пояснением, как они применимы к задаче. Включай ссылку на источник (путь к файлу). **Переводи FPF-термины** в engineering-язык.

### 6. Если явно попросили сохранить в файл

Триггер: в запросе есть «сохрани в файл», «запиши в файл», «пиши в md-файл» или аналог. Без этого триггера — не создавай файл, только показывай результат в чате (шаг 5).

Путь: `DS-strategy/inbox/fpf-local/YYYY-MM-DD-<slug>.md` (`mkdir -p` при необходимости — директории может не быть). `<slug>` — краткое kebab-case из темы запроса или найденного кода паттерна (2-4 слова, не выдумывай — бери из формулировки запроса или из найденного паттерна), например `a7-role-method-work.md`.

**Один вопрос (тема разговора) — один файл, не один вызов скилла — один файл:**
- Текущий вызов — уточнение/продолжение темы, на которую `/fpf-local` уже отвечал в этом разговоре (тот же паттерн, тот же вопрос, «а если...», «уточни», «дополни», «а что если...») → **допиши** в существующий файл этой темы новой секцией `\n---\n## Уточнение HH:MM — <кратко о вопросе>\n`, не перезаписывай и не создавай новый файл.
- Текущий вызов — новая, не связанная с предыдущей тема → **новый файл** с новым `<slug>`.
- Критерий «та же тема» — по смыслу разговора, не по формальным признакам (совпадению кода паттерна недостаточно и не обязательно: разные паттерны в одном уточняющем вопросе — та же тема; тот же паттерн, но с нуля в несвязанном контексте — новая тема).
- Область действия правила — этот разговор. Если сессия свежая и в контексте нет более раннего вызова `/fpf-local` по этой теме — веди себя как с новой темой, даже если по смыслу она пересекается со старым файлом на диске.

Содержимое файла (первая секция или единственная, если файл новый): запрос дословно, источник (путь + диапазон строк в FPF-Spec.md), режим (шаг 1), затем сам результат из шага 5. Место выбрано пилотом (не WP-context, не memory/, не FPF-Practice-Log.md) — сырые находки из `inbox/fpf-local/`, годные для практики, пилот поднимает в `DS-strategy/docs/FPF-Practice-Log.md` вручную.

<!-- USER-SPACE -->
<!-- /USER-SPACE -->
