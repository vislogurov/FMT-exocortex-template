#!/usr/bin/env bash
# .githooks/commit-msg
# Part of WP-453: Конвейер обновления раздела 7 guide
#
# ВЫЗОВ Т3 ПЕРЕНЕСЁН В post-commit (2026-07-31, пир-сессия 2026-07-31-22):
# на стадии commit-msg новый коммит ещё не создан — `git rev-parse HEAD` давал
# SHA ПРЕДЫДУЩЕГО коммита, и падение другого хука после записи оставляло ложный
# триггер в section-state.yaml. Файл оставлен как маркер; логика — в post-commit.

exit 0
